{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "process",
            "isTestCommand": true,
            "command": "pnpm",
            "label": "test",
            "args": ["test", "${relativeFile}", "--", "--watch"]
        }
    ]
}
